name: Custom Action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: hello custom action
        uses: ahmadjubair101/CI-CD@main
        with:
          who-to-greet: "Ahmad Jubair"
